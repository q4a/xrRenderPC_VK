/*!
 * \file    model_pool.cc
 * \brief   Mesh manager implementation
 */

#include "frontend/model_pool.h"


//-----------------------------------------------------------------------------
RenderVisual *
ModelPool::Create
        ( const std::string &name
        , IReader           *rstream
        )
{
    VERIFY(name.size());

    return nullptr;
}
